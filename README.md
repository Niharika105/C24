# C24
Adding of string to a name 
#include<stdio.h>
#include<string.h>
int main()
{
    char name[30];
    printf("enter the first name:\n");
    scanf("%s",name);
    printf("Hello %s",&name);
    return 0;
}
